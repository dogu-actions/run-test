import { ActionKit, HostPaths } from '@dogu-tech/action-kit';
import { spawnSync } from 'child_process';
import path from 'path';
import fs from 'fs';

ActionKit.run(async ({ options, logger, deviceHostClient, consoleActionClient }) => {});
